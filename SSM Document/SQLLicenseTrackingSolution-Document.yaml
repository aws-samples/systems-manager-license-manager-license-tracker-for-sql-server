description: SQL License Tracking Solution
schemaVersion: '0.3'
assumeRole: '{{ AutomationAssumeRole }}'
outputs:
  - updateInventory.result
  - updateLicenseManagerConfiguration.result
parameters:
  InstanceId:
    type: String
    description: '(Required) The ID of the EC2 instance'
    allowedPattern: '^[m]{0,1}i-[a-z0-9]{8,17}$'
  Region:
    type: String
    description: '(Required) Specify the AWS Region where you are deploying this document'
    default: '{{global:REGION}}'
  AccountId:
    type: String
    description: '(Required) Specify the AWS Account ID where you are deploying this document'
    default: '{{global:ACCOUNT_ID}}'
  SQLEELicenseConfiguration:
    type: String
    description: '(Optional) License Configuration ARN for SQL Enterprise Edition'
    default: ''
    allowedPattern: '^$|arn:aws:license-manager:[a-z0-9-]*[-]\d:[0-9]{12}:license-configuration:lic-[a-z0-9]*$'
  SQLEXPLicenseConfiguration:
    type: String
    description: '(Optional) License Configuration ARN for SQL Express Edition'
    default: ''
    allowedPattern: '^$|arn:aws:license-manager:[a-z0-9-]*[-]\d:[0-9]{12}:license-configuration:lic-[a-z0-9]*$'
  SQLSTDLicenseConfiguration:
    type: String
    description: '(Optional) License Configuration ARN for SQL Standard Edition'
    default: ''
    allowedPattern: '^$|arn:aws:license-manager:[a-z0-9-]*[-]\d:[0-9]{12}:license-configuration:lic-[a-z0-9]*$'
  SQLWEBLicenseConfiguration:
    type: String
    description: '(Optional) License Configuration ARN for SQL Web Edition'
    default: ''
    allowedPattern: '^$|arn:aws:license-manager:[a-z0-9-]*[-]\d:[0-9]{12}:license-configuration:lic-[a-z0-9]*$'
  SQLDEVLicenseConfiguration:
    type: String
    description: '(Optional) License Configuration ARN for SQL Developer Edition'
    default: ''
    allowedPattern: '^$|arn:aws:license-manager:[a-z0-9-]*[-]\d:[0-9]{12}:license-configuration:lic-[a-z0-9]*$'
  AutomationAssumeRole:
    type: String
    description: '(Required) The IAM role for this execution. If no role is specified, then AWS Systems Manager will be unable to complete the execution of this document.'
    default: 'arn:aws:iam::{{global:ACCOUNT_ID}}:role/SQLLicenseTrackingSolution-Role'
mainSteps:
  - name: assertInstanceEligibility
    action: 'aws:executeScript'
    description: 'This step checks if the EC2 instance is eligible for this document. The two criteria for this are - (i) is a SSM managed instance and is currently online, (ii) is running on a Windows operating system'
    inputs:
      Runtime: python3.7
      Handler: script_handler
      Script: |-
        import sys
        import boto3
        import json
        import botocore

        def script_handler(events, context):
          instanceId = events['instanceId']

          client = boto3.client('ssm')
          
          response = client.describe_instance_information(
            InstanceInformationFilterList=[
                {
                    'key': 'InstanceIds',
                    'valueSet': [
                        instanceId,
                    ]
                },
            ]
          )
          
          if response["InstanceInformationList"]:
            if response["InstanceInformationList"][0]['PingStatus'] == 'Online' and response["InstanceInformationList"][0]['PlatformType'] == 'Windows':
              status = f"{instanceId} is eligible for this automation execution"
            else:
              raise Exception(f"{instanceId} currently appears to be unavilable for this automation")
          else:
            raise Exception(f"{instanceId} is ineligible for this automation")
                            
          return status
      InputPayload:
        instanceId: '{{ InstanceId }}'
    nextStep: removeInventoryAndLicenseConfigData
    isCritical: false
  - name: removeInventoryAndLicenseConfigData
    action: 'aws:executeScript'
    description: 'This step performs two clean up tasks. Firstly, deletes all the metadata associated with the custom inventory type (https://docs.aws.amazon.com/systems-manager/latest/userguide/sysman-inventory-custom.html) created for MSSQL [“Custom:SQLServer”] on all the EC2 instances. Secondly, we remove any association to the license configuration if they exist. This gives us an opportunity to rehydrate the latest data in to both SSM Inventory and License Manager in case changes have been made'
    inputs:
      Runtime: python3.7
      Handler: script_handler
      Script: |-
        import boto3
        import json
        import botocore

        def script_handler(events, context):

          lmClient = boto3.client('license-manager')
          ssmClient = boto3.client('ssm')
          
          accountId = events['accountId'] 
          region = events['region'] 
          instanceId = events['instanceId'] 
          sqlEELicenseConfig = events['sqlEELicenseConfig'] 
          sqlSTDLicenseConfig = events['sqlSTDLicenseConfig']
          sqlEXPLicenseConfig = events['sqlEXPLicenseConfig']
          sqlWEBLicenseConfig = events['sqlWEBLicenseConfig']
          sqlDEVLicenseConfig = events['sqlDEVLicenseConfig']

          ec2ARN = f"arn:aws:ec2:{region}:{accountId}:instance/{instanceId}"
          licenseConfigARN = ''
          
          try:
            lmResponse = lmClient.list_license_specifications_for_resource(
                          ResourceArn=ec2ARN,
                          MaxResults=10
                          )
                          
            if lmResponse['LicenseSpecifications']:
              for spec in lmResponse['LicenseSpecifications']:
                if spec['LicenseConfigurationArn']:
                  if (sqlEELicenseConfig and spec['LicenseConfigurationArn'] == sqlEELicenseConfig):
                    licenseConfigARN = sqlEELicenseConfig
                    
                  elif (sqlSTDLicenseConfig and spec['LicenseConfigurationArn'] == sqlSTDLicenseConfig):
                    licenseConfigARN = sqlSTDLicenseConfig
                                  
                  elif (sqlEXPLicenseConfig and spec['LicenseConfigurationArn'] == sqlEXPLicenseConfig):
                    licenseConfigARN = sqlEXPLicenseConfig
                    
                  elif (sqlWEBLicenseConfig and spec['LicenseConfigurationArn'] == sqlWEBLicenseConfig):
                    licenseConfigARN = sqlWEBLicenseConfig
                    
                  elif (sqlDEVLicenseConfig and spec['LicenseConfigurationArn'] == sqlDEVLicenseConfig):
                    licenseConfigARN = sqlDEVLicenseConfig
                                  
                if licenseConfigARN:
                  updateResponse = lmClient.update_license_specifications_for_resource(
                                  ResourceArn=ec2ARN,
                                  RemoveLicenseSpecifications=[
                                  {
                                      'LicenseConfigurationArn': licenseConfigARN
                                  }]);
                  licenseConfigARN = ''                
                  
          except botocore.exceptions.ClientError as error:
            if error.response['Error']['Code'] == 'InvalidParameterValueException':
              print('License specification does not exist for this resource. Skipping this step')
            else:
              print('Unknown error')
                                
          try:
            ssmResponse = ssmClient.delete_inventory(
              TypeName='Custom:SQLServer'
            )
          
          except botocore.exceptions.ClientError as error:
            print("Custom Inventory deletion error: ",error)
      InputPayload:
        instanceId: '{{ InstanceId }}'
        accountId: '{{ AccountId }}'
        region: '{{ Region }}'
        sqlEELicenseConfig: '{{ SQLEELicenseConfiguration }}'
        sqlSTDLicenseConfig: '{{ SQLSTDLicenseConfiguration }}'
        sqlEXPLicenseConfig: '{{ SQLEXPLicenseConfiguration }}'
        sqlWEBLicenseConfig: '{{ SQLWEBLicenseConfiguration }}'
        sqlDEVLicenseConfig: '{{ SQLDEVLicenseConfiguration }}'
    nextStep: isSQLServerInstalled
    isCritical: false
  - name: isSQLServerInstalled
    description: 'In this step we check if MSSQL is installed and if it exists then retrieve the instance(s) details running on the EC2 instance using Windows Registry. The output captures the Name, Edition and Version of the MSSQL instances'
    isEnd: false
    action: 'aws:runCommand'
    onFailure: Abort
    inputs:
      DocumentName: AWS-RunPowerShellScript
      InstanceIds:
        - '{{ InstanceId }}'
      Parameters:
        commands:
          - |
            $found = "false"
            $list = New-Object System.Collections.ArrayList
            $pathExists = Test-Path -Path "HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server"
            if ($pathExists)
            {
              $found = "true"
              $server = (get-itemproperty -ErrorAction Stop 'HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server').InstalledInstances
              foreach ($s in $server) 
              {
                  $p = (Get-ItemProperty 'HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\Instance Names\SQL').$s
                  $o = [pscustomobject]@{
                   Version = (Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\$p\Setup").Version
                   Edition = (Get-ItemProperty "HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\$p\Setup").Edition
                   Name = $p
                  }
                  $server_info_list = $list.Add($o)
              }
              $jsonList = ConvertTo-Json -InputObject $list
              return $jsonList
            }
            else
            {
              return $found
            }
    outputs:
      - Name: result
        Type: String
        Selector: $.Output
  - name: foundSQLServerInstalledBranch
    description: 'This step will branch (https://docs.aws.amazon.com/systems-manager/latest/userguide/automation-action-branch.html) out based on the evaluation of the previous step, defaulting to the next step if MSSQL exists on the EC2 instance otherwise exiting if unavailable'
    isEnd: false
    onFailure: Abort
    action: 'aws:branch'
    inputs:
      Choices:
        - NextStep: exitIfNoSqlServerFound
          Variable: '{{isSQLServerInstalled.result}}'
          Contains: 'false'
      Default: updateInventory
  - name: updateInventory
    description: 'Using the output of step 3 (metadata) we will update SSM Inventory with a custom inventory of type “Custom:SQLServer” for the EC2 instance'
    action: 'aws:executeScript'
    inputs:
      Runtime: python3.7
      Handler: script_handler
      Script: |-
        import boto3
        import json
        from datetime import datetime, timezone
          
        ssm = boto3.client('ssm')

        def script_handler(events, context):
          sqlInfo = events['sqlInfo']
          infoList = list(eval(sqlInfo))
          timestamp = datetime.now(timezone.utc).isoformat()[:-13] + 'Z'
          
          response = ssm.put_inventory(
            InstanceId=events['instanceId'],
            Items=[
                {
                    'TypeName': 'Custom:SQLServer',
                    'SchemaVersion': '1.0',
                    'CaptureTime': timestamp,
                    'Content': infoList
                },
            ]
          )
          
          if "HTTPStatusCode" in response["ResponseMetadata"]:
            httpStatusCode = response["ResponseMetadata"]["HTTPStatusCode"]
            if httpStatusCode == 200:
              status = 'Successfully updated Inventory'
            else:
              status = 'Failed to update Inventory'
          else:
            raise Exception(f"It appears that this step couldn't be completed due to an unknown error. Please check the logs for more details")
            
          return {'message': status}
      InputPayload:
        instanceId: '{{InstanceId}}'
        sqlInfo: '{{isSQLServerInstalled.result}}'
    isEnd: false
    outputs:
      - Name: result
        Selector: $.Payload.message
        Type: String
    nextStep: updateLicenseManagerConfiguration
  - name: updateLicenseManagerConfiguration
    description: 'In this last step we determine the highest edition of SQL installed and accordingly update the License Manager Configuration associated with the EC2 instance'
    action: 'aws:executeScript'
    isEnd: true
    onFailure: Abort
    inputs:
      Runtime: python3.7
      Handler: script_handler
      InputPayload:
        accountId: '{{ AccountId }}'
        region: '{{ Region }}'
        instanceId: '{{ InstanceId }}'
        sqlInfo: '{{isSQLServerInstalled.result}}'
        sqlEELicenseConfig: '{{ SQLEELicenseConfiguration }}'
        sqlSTDLicenseConfig: '{{ SQLSTDLicenseConfiguration }}'
        sqlEXPLicenseConfig: '{{ SQLEXPLicenseConfiguration }}'
        sqlWEBLicenseConfig: '{{ SQLWEBLicenseConfiguration }}'
        sqlDEVLicenseConfig: '{{ SQLDEVLicenseConfiguration }}'
      Script: |-
        import boto3
        import json

        def script_handler(events, context):

          sqlInfo = events['sqlInfo']
          jsonSqlInfo = json.loads(sqlInfo)
          
          editions = {1: 'Enterprise Edition',
                2: 'Standard Edition',
                3: 'Web Edition',
                4: 'Express Edition',
                5: 'Developer Edition'}
                
          highestEdition = ''
          for key, value in editions.items():

            for instance in jsonSqlInfo:
            
              if (instance["Edition"] == value):
                highestEdition = value
                break
            if highestEdition:
              break
              
          lmClient = boto3.client('license-manager')
          
          accountId =  events['accountId'] 
          region = events['region'] 
          instanceId = events['instanceId'] 
          sqlEELicenseConfig = events['sqlEELicenseConfig'] 
          sqlSTDLicenseConfig = events['sqlSTDLicenseConfig'] 
          sqlEXPLicenseConfig = events['sqlEXPLicenseConfig'] 
          sqlWEBLicenseConfig = events['sqlWEBLicenseConfig'] 
          sqlDEVLicenseConfig = events['sqlDEVLicenseConfig'] 
          
          ec2ARN = f"arn:aws:ec2:{region}:{accountId}:instance/{instanceId}"
          licenseConfigARN = ''
          
          if highestEdition == "Enterprise Edition" and sqlEELicenseConfig:
            licenseConfigARN = sqlEELicenseConfig
             
          elif highestEdition == "Standard Edition" and sqlSTDLicenseConfig:
            licenseConfigARN =  sqlSTDLicenseConfig
            
          elif highestEdition == "Web Edition" and sqlWEBLicenseConfig:
            licenseConfigARN =  sqlWEBLicenseConfig 
            
          elif highestEdition == "Express Edition" and sqlEXPLicenseConfig:
            licenseConfigARN  = sqlEXPLicenseConfig
                  
          elif highestEdition == "Developer Edition" and sqlDEVLicenseConfig:
            licenseConfigARN  = sqlDEVLicenseConfig
            
          else:
            raise Exception(f"Sorry, No {highestEdition} License Configuration ARN has been supplied")
            
            
          if licenseConfigARN:
            response = lmClient.update_license_specifications_for_resource(
                ResourceArn=ec2ARN,
                AddLicenseSpecifications=[
                {
                    'LicenseConfigurationArn': licenseConfigARN
                }]);
            licenseConfigARN = ''
          
            if "HTTPStatusCode" in response["ResponseMetadata"]:
              httpStatusCode = response["ResponseMetadata"]["HTTPStatusCode"]
              if httpStatusCode == 200:
                status = 'Successfully updated License Manager Configuration'
              else:
                status = 'Failed to update License Manager Configuration'
            else:
              raise Exception(f"It appears that this step couldn't be completed due to an unknown error. Please check the logs for more details")
          
          return {'message': status}
    outputs:
      - Name: result
        Selector: $.Payload.message
        Type: String  
  - name: exitIfNoSqlServerFound
    action: 'aws:runCommand'
    isEnd: true
    onFailure: Abort
    inputs:
      DocumentName: AWS-RunPowerShellScript
      Parameters:
        commands:
          - |
            $message= 'No SQL Instances found'
            return $message
      InstanceIds:
        - '{{ InstanceId }}'
    outputs:
      - Name: message
        Selector: $.Output
        Type: String
    description: 'This step is triggered if there are no SQL instances found.'